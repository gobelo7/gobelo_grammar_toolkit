// src/App.jsx
// Application root. GrammarAdmin carries its own provider tree.
// Add routing here if a public-facing student mode is added later.
import GrammarAdmin from "./admin/GrammarAdmin";

export default function App() {
  return <GrammarAdmin />;
}
