import React from 'react';
import ReactDOM   from 'react-dom/client';
import App from './GGT_Grammar_Admin.jsx';

const root = ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GGT_Grammar_Admin  />
  </React.StrictMode>
);

